{
  "name": "bird-monitoring-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm i && concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm i && npm start",
    "start:frontend": "cd frontend && npm i && npm start"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}